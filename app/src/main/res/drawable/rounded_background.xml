<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" /> <!-- El color de fondo del TextView -->
    <corners android:radius="8dp" /> <!-- El radio de las esquinas -->
    <stroke android:width="2dp"
        android:color="#FFF" /> <!-- Ancho y color del borde (opcional) -->
</shape>